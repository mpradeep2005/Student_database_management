install the requirement in requirement.txt
to run the application enter the command  "uvicorn service:app --reload"
if your using a virtual environment activate it and then run
the uvicorn port number is  http://127.0.0.1:8000
and the postgresSQl port number is 7070.